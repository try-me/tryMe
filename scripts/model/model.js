function addBadges() {
  //when user gets a certain number of points/challenges completed award badge
};

function addPoints() {
  //when user finishes challenges/events update the points in object
};

function addFriends() {
  //add friends to friends property
};

function sendChallenge() {
  //send a challenge to another user
};
